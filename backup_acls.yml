---
- name: Backup ACLS
  hosts: all
  gather_facts: yes

  tasks:
    - name: Gather the existing configuration
      arista.eos.eos_acls:
        state: gathered
      register: aacls

    - ansible.builtin.debug:
        msg: "{{ aacls }}"
